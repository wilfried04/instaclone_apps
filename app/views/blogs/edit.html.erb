<div class="container">
  <% if logged_in? %>
    <% if @blog.user.id == current_user.id %>
        <%=render 'form' %>
    <% else %>
    <span class="badge badge-pill badge-danger"><%= link_to "Log in", new_session_path%></span>
    <span class="badge badge-pill badge-warning"><%= link_to "Sign Up", new_user_path%></span>
   <% end %>
 <% else %>
   <span class="badge badge-pill badge-danger"><%= link_to "Log in", new_session_path%></span>
  <span class="badge badge-pill badge-warning"><%= link_to "Sign Up", new_user_path%></span>
 <% end %>
</div>
